extends layout
block content
  .container
      h2 
        p
         | HİLMİ COŞKUN HAVA DURUM SORGULAMA EKRANI
      br
      br
      .row
        .col-md-3
          form(method="post", action="get_weather")
            .form-group
              label.sr-only(for="city") City Name
              input.form-control( name="city" type="text" placeholder="Enter City Name (for ex: New York)" required='required')
            button.btn.btn-success(type="submit") Bilgileri Al :)
      if error
        br
        .alert.alert-danger #{error}
      if weather
        .row
          .col-md-5
            p
            br
            .panel.panel-primary
              .panel-heading Sorgulanan Şehir : #{weather.name}, #{weather.sys.country}
              .panel-body
               p Şu anki Hava Sıcaklığı = #{weather.main.temp} !{units}, #{weather.weather[0].description}
               p Nem = #{weather.main.humidity} &#37;
               p Min = #{weather.main.temp_min} !{units}
               p Max = #{weather.main.temp_max} !{units}